# P3 measurement — MiniMax-H3 per-prompt program preparation cost (RTX 5080)

Compiler build `/home/alex/diffusion-compiler/build-5080-release` (as-is). Checkpoint
`~/.serenity/models/checkpoints/MiniMax-H3/FL2VA`. Geometry: the frozen T2VA
832x480x124 contract (video rows 14430, audio rows 414, 2 timestep tables,
streamed constants, exact cuDNN attention). Prompt: 35 tokens (prompt-short.txt).
Serialized under /tmp/dc-gpu.lock. Timings are single runs, wall seconds.

| stage | fresh | rebind from the s439/t439 sealed fixtures |
|---|---:|---:|
| diftokenize | 0.12 | — |
| difcondition program (--sequence 35) | 0.06 | — |
| difcondition bundle | 184.37 (hashes 14 text-encoder shards) | 0.077 (`--sealed-bundle`) |
| difc make-h3-denoiser (14430 414 35 2 streamed cudnn) | 0.01 | — |
| difweights make-h3-denoiser-bundle | 228.01 (hashes 13 transformer shards) | 0.070 (`rebind-h3-denoiser-bundle`) |

Rebound bundles are byte-identical to the freshly sealed ones (`cmp`). The
modulation cache depends on steps and block count only, not on the prompt.

Decision: serve arbitrary prompts by per-request program generation plus
rebind from one base sealed bundle per (conditioner, denoiser geometry). Cost
≈ 0.3 s per request. No pre-built token-count matrix is needed.
